# Pin Gradle's own JVM (and therefore `./gradlew run`) to the JavaFX-bundled
# Zulu build. Without this, Gradle may pick a plain JDK on this machine
# (Microsoft OpenJDK 25 or Oracle JDK 21, neither bundles JavaFX) and the
# app would fail with "JavaFX runtime components are missing".
org.gradle.java.home=/Users/shrad1601/.sdkman/candidates/java/25.0.3.fx-zulu
